using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Warriou : Player
{

    // Start is called before the first frame update
    void Start()
    {
        base.Start();
        characterType = "Warriou";
    }

    // Update is called once per frame
    void Update()
    {
        base.Update();
    }
}
